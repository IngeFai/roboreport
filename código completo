<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RoboReport - Gestión de Incidentes</title>
    <link rel="stylesheet" href="estilo.css"> <!-- Enlace al archivo CSS -->
</head>
<body>
    <header>
        <h1>Bienvenido a RoboReport</h1>
    </header>

    <main>
        <!-- Contenedor que agrupa los cuadros de inicio de sesión y crear cuenta -->
        <div id="contenedor-cajas">
            <!-- Sección de Inicio de Sesión -->
            <section id="login">
                <h2>Iniciar Sesión</h2>
                <form action="#" method="post">
                    <label for="email">Correo Electrónico:</label>
                    <input type="email" id="email" name="email" required>
                    <br>
                    <label for="password">Contraseña:</label>
                    <input type="password" id="password" name="password" required>
                    <br>
                    <button type="submit">Iniciar Sesión</button>
                </form>
                <p><a href="#">Olvidaste tu contraseña?</a></p>
            </section>

            <!-- Sección de Crear Cuenta -->
            <section id="crear-cuenta">
                <h2>Crear una Cuenta Nueva</h2>
                <form action="registro.php" method="POST">
                    <label for="nombre">Nombre Completo:</label>
                    <input type="text" id="nombre" name="nombre" required>
                    <br>
                    <label for="email-registro">Correo Electrónico:</label>
                    <input type="email" id="email-registro" name="email-registro" required>
                    <br>
                    <label for="password-registro">Contraseña:</label>
                    <input type="password" id="password-registro" name="password-registro" required>
                    <br>
                    <label for="confirm-password">Confirmar Contraseña:</label>
                    <input type="password" id="confirm-password" required>
                    <br>
                    <button type="submit">Crear Cuenta</button>
                </form>                
            </section>
        </div>
        

        <!-- Menú de Navegación para información -->
        <nav id="menu-informacion">
            <ul>
                <li><a href="#">Quiénes Somos</a></li>
                <li><a href="#">Contacto</a></li>
                <li><a href="#">PQRS</a></li>
                <li><a href="#">Acerca de Nosotros</a></li>
            </ul>
        </nav>
    </main>

    <footer>
        <!-- Imagen del escudo de la Policía Nacional a la izquierda -->
        <img src="EscudoPonal.jpg" alt="Escudo Policía Nacional">
        
        <!-- Texto en el centro -->
        <div class="footer-text">
            <p>© 2024 RoboReport. Todos los derechos reservados. Faiver Ordóñez</p>
            <p><a href="#">Política de Privacidad</a> | <a href="#">Términos y Condiciones</a> | <a href="#">Contacto</a></p>
        </div>
        
        <!-- Imagen del escudo de la Secretaría de Convivencia y Justicia a la derecha -->
        <img src="EscudoSCJ.png" alt="Escudo Secretaría de Convivencia y Justicia">
        
        <!-- Contenedor de los cuadros animados -->
        <div class="footer-squares">
            <div class="square square1"></div>
            <div class="square square2"></div>
            <div class="square square3"></div>
            <div class="square square4"></div>
        </div>
    </footer>








  y /* Estilo general del cuerpo */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f0f0; /* Fondo claro para el cuerpo */
    color: #333; /* Texto oscuro para mejor legibilidad */
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* Encabezado con imagen de fondo */
header {
    background-image: url('Bogotá.jpg');
    background-size: cover;
    background-position: center;
    color: white;
    padding: 10px 0;
    width: 100%;
    height: 250px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    position: relative;
}

/* Menú de navegación arriba del título en el encabezado */
#menu-informacion {
    display: flex;
    justify-content: center; /* Centrar el menú */
    width: 100%;
    margin-bottom: 10px; /* Añade margen debajo del menú para separarlo del título */
    gap: 20px; /* Espacio entre los enlaces */
    position: absolute;
    top: 20px; /* Posiciona el menú en la parte superior del encabezado */
}

/* Estilo para los enlaces del menú de navegación */
#menu-informacion ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: contents;
}

#menu-informacion li {
    margin: 0 20px; /* Ajusta el espacio entre los ítems */
}

#menu-informacion a {
    background-color: #0307ee; /* Fondo azul oscuro para los botones/cajones */
    color: white;
    padding: 10px 20px; /* Espaciado para parecer botones */
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    transition: background-color 0.3s ease;
}

#menu-informacion a:hover {
    background-color: #00bfff; /* Cambia de color al pasar el mouse */
    text-decoration: underline;
}

/* Estilo del título en el encabezado */
header h1 {
    background-color: rgba(0, 0, 0, 0.7);
    padding: 5px 20px;
    border-radius: 8px;
    color: #FFFFFF;
    font-size: 40px;
    letter-spacing: 2px;
    text-align: center;
    margin-top: 70px; /* Añadimos margen superior para que el título esté más abajo */
}



/* Estilo del título en el encabezado */
header h1 {
    background-color: rgba(0, 0, 0, 0.7); /* Fondo oscuro semitransparente */
    padding: 10px 30px;
    border-radius: 8px;
    color: #FFFFFF;
    font-size: 56px;
    letter-spacing: 2px;
    text-align: center;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
}

/* Fondo degradado con efecto más llamativo */
#contenedor-cajas {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    padding: 50px 0;
    background: linear-gradient(45deg, rgb(99, 3, 3), rgb(0, 255, 76), #09f, #0cf); /* Colores llamativos */
    background-size: 400% 400%; /* Escalamos para darle más espacio al movimiento */
    animation: fondoDinamico 08s ease-in-out infinite; /* Animación que dura 8 segundos y se repite infinitamente */
    box-shadow: inset 0px 4px 12px rgba(0, 0, 0, 0.3); /* Sombra interna */
}

/* Animación para el fondo degradado llamativo */
@keyframes fondoDinamico {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* Estilo para las secciones de login y crear cuenta */
#login, #crear-cuenta {
    background-color: #ffffff;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    width: 100%;
    max-width: 400px;
    text-align: center;
    margin: 0 15px; /* Espaciado entre los cuadros */
    transition: transform 0.3s ease;
}

#login:hover, #crear-cuenta:hover {
    transform: translateY(-5px); /* Efecto hover */
}

/* Estilo de los títulos y etiquetas en los formularios */
#login h2, #crear-cuenta h2 {
    color: #333333; /* Color oscuro para destacar mejor */
    font-size: 26px;
    margin-bottom: 25px;
}

label {
    display: block;
    margin-bottom: 8px;
    font-size: 16px;
    color: #333333; /* Texto oscuro */
}

/* Centrar los campos en los formularios */
#login input, #crear-cuenta input {
    display: block;
    margin: 0 auto;
    width: 100%;
    max-width: 350px; /* Un poco más ancho para mayor visibilidad */
    padding: 12px;
    margin: 15px 0;
    border: 1px solid #cccccc; /* Borde más claro */
    background-color: #f9f9f9;
    color: #333333;
    border-radius: 5px;
    font-size: 16px;
}

input::placeholder {
    color: #999999; /* Color de los placeholders para mejor visibilidad */
}

/* Botones */
button {
    background-color: #007BFF;
    color: white;
    border: none;
    padding: 12px;
    width: 100%;
    max-width: 350px;
    border-radius: 5px;
    font-size: 26px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #0056b3;
}

/* Footer con imágenes, enlaces y texto */
footer {
    padding: 5px 5px;
    background-color: #fcf7f7;
    color: #1d1d1d;
    width: 100%;
    position: fixed;
    bottom: 0;
    box-shadow: 0 -4px 6px rgba(0, 0, 0, 0.3);
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 150px; /* Aumentar la altura del footer */
    overflow: hidden; /* Ocultar cualquier desbordamiento */
    position: relative; /* Necesario para los cuadros animados */
}

/* Imágenes del footer más grandes */
footer img {
    height: 100%;
    width: auto;
}

/* Estilo del texto dentro del footer */
.footer-text {
    flex-grow: 1;
    text-align: center;
    z-index: 2; /* Asegurar que el texto esté por encima de los cuadros */
}

.footer-text p {
    margin: 0;
    color: #333333;
    font-size: 20px;
}

.footer-text a {
    color: #007BFF;
    text-decoration: none;
    margin: 0 10px;
}

.footer-text a:hover {
    text-decoration: underline;
}

/* Cuadros animados en el footer */
.footer-squares {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    z-index: 1; /* Aseguramos que los cuadros estén detrás del texto e imágenes */
}

.square {
    position: absolute;
    width: 40px; /* Tamaño de los cuadros */
    height: 40px;
    background: rgba(0, 123, 255, 0.7); /* Azul con opacidad */
    animation: moveSquare 5ms linear infinite; /* Animación continua */
    border-radius: 8px; /* Bordes redondeados para hacer los cuadros más elegantes */
}

/* Animación de los cuadros moviéndose */
@keyframes moveSquare {
    0% {
        transform: translateX(0) translateY(0);
    }
    100% {
        transform: translateX(100vw) translateY(100vh); /* Movimiento a lo largo de la pantalla */
    }
}

/* Diferentes posiciones de inicio de los cuadros */
.square1 {
    top: 0%;
    left: 50%;
    animation-duration: 10s;
}

.square2 {
    top: 40%;
    left: -20%;
    animation-duration: 5s;
}

.square3 {
    top: 60%;
    left: -30%;
    animation-duration: 5s;
}

.square4 {
    top: 80%;
    left: -15%;
    animation-duration: 5s;
}





  // Validación del formulario de inicio de sesión
document.getElementById("login").addEventListener("submit", function(event) {
    var email = document.getElementById("email").value;
    var password = document.getElementById("password").value;

    if (email === "" || password === "") {
        alert("Todos los campos son obligatorios");
        event.preventDefault(); // Evitar que el formulario se envíe si los campos están vacíos
    } else if (!validateEmail(email)) {
        alert("Por favor, ingresa un correo electrónico válido.");
        event.preventDefault();
    }
});

// Validación del formulario de registro de cuenta
document.getElementById("crear-cuenta").addEventListener("submit", function(event) {
    var nombre = document.getElementById("nombre").value;
    var emailRegistro = document.getElementById("email-registro").value;
    var passwordRegistro = document.getElementById("password-registro").value;
    var confirmPassword = document.getElementById("confirm-password").value;

    if (nombre === "" || emailRegistro === "" || passwordRegistro === "" || confirmPassword === "") {
        alert("Todos los campos son obligatorios");
        event.preventDefault();
    } else if (!validateEmail(emailRegistro)) {
        alert("Por favor, ingresa un correo electrónico válido.");
        event.preventDefault();
    } else if (passwordRegistro !== confirmPassword) {
        alert("Las contraseñas no coinciden.");
        event.preventDefault();
    }
});

// Función para validar el formato del correo electrónico
function validateEmail(email) {
    var re = /\S+@\S+\.\S+/; // Expresión regular para validar el correo
    return re.test(email);
}




  <?php
// Mostrar errores de PHP
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

// Conexión a la base de datos
$conn = new mysqli("localhost", "root", "", "roboreport");

// Verificar la conexión
if ($conn->connect_error) {
    die("Error en la conexión: " . $conn->connect_error);
}

// Verificar si el formulario fue enviado
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $nombre = $_POST['nombre'];
    $email = $_POST['email-registro'];
    $password = $_POST['password-registro'];

    // Verificar si el correo ya está registrado
    $checkEmailQuery = "SELECT * FROM usuarios WHERE email = ?";
    $stmt = $conn->prepare($checkEmailQuery);
    $stmt->bind_param("s", $email);
    $stmt->execute();
    $result = $stmt->get_result();

    if ($result->num_rows > 0) {
        echo "El correo electrónico ya está registrado.";
    } else {
        // Insertar el nuevo usuario
        $hashed_password = password_hash($password, PASSWORD_BCRYPT); // Encriptar la contraseña
        $insertQuery = "INSERT INTO usuarios (nombre, email, password) VALUES (?, ?, ?)";
        $stmt = $conn->prepare($insertQuery);
        $stmt->bind_param("sss", $nombre, $email, $hashed_password);

        if ($stmt->execute()) {
            echo "Registro exitoso.";
        } else {
            echo "Error al registrar: " . $stmt->error;
        }
    }

    $stmt->close();
}

$conn->close();


  <?php
echo "PHP está funcionando correctamente.";
